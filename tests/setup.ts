/**
 * tests/setup.ts — global vitest setup file.
 *
 * Imports @testing-library/jest-dom so its matchers (toBeInTheDocument, etc.)
 * are available in all test files. This runs before each test file.
 */
import "@testing-library/jest-dom";
